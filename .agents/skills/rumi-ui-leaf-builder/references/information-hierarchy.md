# Information Hierarchy

Classify each item before writing UI:

- Primary: the user must perceive it to complete the leaf's main flow.
- Secondary: useful at rest but less important than the primary task.
- Contextual: shown only when a state, selection, or disclosure calls for it.

Hierarchy must remain visible in grayscale. Use typography, spacing, position, and grouping before relying on color.
