# Assets

Place application icon files here:

- `icon.png` — used by the system tray (all platforms, 32×32 or 64×64 RGBA PNG)
- `icon.ico` — embedded into the Windows executable via `build.rs` + `winres`

These files are **not** included in the repository. Generate or obtain them
before building a release.
