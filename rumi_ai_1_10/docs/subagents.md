# Utility Subagents

Defaultspack utility subagents cover `vision_ocr`, `tool_selector`, `prompt_compactor`, `context_summarizer`, and `model_router`.

They use role-specific utility model settings when configured and fall back to deterministic structured output when no utility model is available.
