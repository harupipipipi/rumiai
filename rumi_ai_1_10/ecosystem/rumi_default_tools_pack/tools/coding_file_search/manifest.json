{
  "id": "coding_file_search",
  "category": "tool",
  "version": "1",
  "display_name": "Search Files",
  "description": "Search workspace files with a glob pattern.",
  "enabled": true,
  "config": {
    "name": "coding_file_search",
    "summary": "Search workspace files",
    "tags": ["coding", "file", "search"],
    "ui": {
      "group_id": "coding/files/read",
      "group_label": "Read",
      "group_icon": "file",
      "drop_capabilities": ["composer.toggle_chip"],
      "widget_kind": "tool_toggle",
      "settings_fields": [
        {"id": "default_workspace_root", "label": "Default Workspace Root", "type": "text", "default": "", "help": "Durable root used when a workspace root is not provided by the runtime."},
        {"id": "max_results", "label": "Max Results", "type": "number", "default": 200, "min": 1, "max": 5000}
      ]
    },
    "tool_category": "filesystem",
    "action_type": "read",
    "risk": "low",
    "approval_policy": "allow",
    "capability_grants": ["file.read"],
    "schema": {
      "parameters": {
        "type": "object",
        "properties": {
          "pattern": {"type": "string"},
          "directory": {"type": "string"}
        },
        "required": ["pattern"]
      }
    },
    "execution": {"type": "rumi_function", "qualified_name": "defaultspack:coding_file_search"}
  }
}
