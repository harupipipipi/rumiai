{
  "id": "browser_use",
  "category": "tool",
  "version": "1",
  "display_name": "Browser Use",
  "description": "Open URLs, inspect browser/app state, capture screenshots for vision-capable models, and move the virtual AI cursor from screenshot coordinates. If existing Chrome returns a chrome_setting recovery for background entry, stop retrying and report the blocker.",
  "enabled": true,
  "config": {
    "name": "browser_use",
    "summary": "ブラウザ操作。action=context で開いているアプリ/選択ウィンドウ/AIカーソルも確認できます。",
    "tags": ["browser", "vision", "screenshot", "cursor"],
    "schema": {
      "parameters": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "enum": ["session", "open_url", "context", "screenshot", "move", "click", "select_window", "windows", "type", "key", "scroll"],
            "default": "session"
          },
          "url": {"type": "string"},
          "x": {"type": "integer"},
          "y": {"type": "integer"},
          "text": {"type": "string"},
          "key": {"type": "string"},
          "amount": {"type": "integer"},
          "target": {"type": "string"},
          "app": {"type": "string"},
          "title": {"type": "string"},
          "coordinate_space": {"type": "string"},
          "physical": {"type": "boolean"},
          "focus": {"type": "boolean"},
          "dry_run": {"type": "boolean"},
          "approval_token": {"type": "string"},
          "payload": {"type": "object"}
        }
      }
    },
    "execution": {
      "type": "rumi_function",
      "qualified_name": "rumi_default_tools_pack:browser_use"
    },
    "requires_approval": true,
    "ui": {
      "group_id": "browser",
      "group_label": "Browser",
      "composer_label": "Browser Use",
      "composer_icon": "globe",
      "drop_capabilities": ["composer.toggle_chip"],
      "widget_kind": "tool_toggle",
      "settings_fields": [
        {
          "id": "default_target",
          "label": "Default Target",
          "type": "select",
          "default": "active_session",
          "options": [
            {"value": "active_session", "label": "Active Session"},
            {"value": "new_tab", "label": "New Tab"},
            {"value": "isolated_session", "label": "Isolated Session"}
          ],
          "help": "Default browser context for new tasks; request-specific details are supplied at call time."
        },
        {
          "id": "mode",
          "label": "Default Mode",
          "type": "select",
          "default": "inspect_first",
          "options": [
            {"value": "inspect_first", "label": "Inspect First"},
            {"value": "direct", "label": "Direct Action"},
            {"value": "read_only", "label": "Read Only"}
          ],
          "help": "Preferred operating mode before issuing browser actions."
        },
        {
          "id": "safety",
          "label": "Safety Default",
          "type": "select",
          "default": "approval_required",
          "options": [
            {"value": "approval_required", "label": "Require Approval"},
            {"value": "dry_run_first", "label": "Dry Run First"},
            {"value": "read_only", "label": "Read Only"}
          ],
          "help": "Durable safety preference for browser operations."
        },
        {
          "id": "quality",
          "label": "Screenshot Quality",
          "type": "select",
          "default": "standard",
          "options": [
            {"value": "compact", "label": "Compact"},
            {"value": "standard", "label": "Standard"},
            {"value": "high_detail", "label": "High Detail"}
          ],
          "help": "Default screenshot/detail preference for visual inspection."
        }
      ]
    }
  }
}
