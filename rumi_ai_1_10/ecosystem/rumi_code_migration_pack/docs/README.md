# Rumi Code Migration Pack Docs

This pack is intentionally declarative. It can be selected as a setup pack to customize Rumi's behavior, but it does not register tools, routes, stores, daemons, or provider clients.

Required Secrets: None.
Network is none by default.
Handoff packets are the only external effect.
