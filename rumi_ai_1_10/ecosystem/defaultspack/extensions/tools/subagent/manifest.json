{
  "id": "subagent",
  "category": "tool",
  "version": "1",
  "display_name": "Subagent",
  "description": "Delegate a bounded task to a child conversation and link it under the parent chat.",
  "enabled": true,
  "config": {
    "name": "subagent",
    "summary": "サブエージェント",
    "tags": ["agent", "subagent", "delegation"],
    "schema": {
      "parameters": {
        "type": "object",
        "properties": {
          "task": {"type": "string"},
          "title": {"type": "string"},
          "model": {"type": "string"},
          "agent_id": {"type": "string"}
        },
        "required": ["task"]
      }
    },
    "execution": {"type": "local"},
    "ui": {
      "group_id": "agent",
      "group_label": "Agent",
      "composer_label": "Subagent",
      "composer_icon": "bot",
      "widget_kind": "tool_toggle"
    }
  }
}
