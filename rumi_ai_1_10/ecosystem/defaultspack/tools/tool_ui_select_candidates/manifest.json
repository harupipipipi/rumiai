{
  "id": "tool_ui_select_candidates",
  "description": "Select accepted recursive UI component bundles after hard-fail filtering and compression scoring.",
  "config": {
    "tool_id": "tool_ui_select_candidates",
    "name": "tool_ui_select_candidates",
    "summary": "Select accepted UI candidates.",
    "tool_category": "frontend",
    "action_type": "local",
    "write_action": true,
    "requires_approval": true,
    "risk": "high",
    "tags": ["frontend", "ui", "selection", "recursive-ui-compiler"],
    "schema": {"parameters": {"type": "object", "additionalProperties": true, "required": ["ui_tree"], "properties": {"ui_tree": {"type": "object"}}}},
    "execution": {"type": "local", "handler": "domain.tool.ui_compiler_tools:ui_select_candidates"}
  }
}
