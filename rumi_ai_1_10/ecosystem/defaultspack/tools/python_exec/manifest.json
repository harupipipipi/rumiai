{
  "id": "python_exec",
  "description": "Request approved Python code/script execution in the managed sandbox runtime; fail closed when no provider is ready.",
  "config": {
    "tool_id": "python_exec",
    "name": "Python Exec",
    "summary": "Request approved Python code/script execution in the managed sandbox runtime; fail closed when no provider is ready.",
    "tool_category": "sandbox",
    "action_type": "execute",
    "write_action": true,
    "requires_approval": true,
    "risk": "high",
    "tags": [
      "sandbox",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "sandbox",
      "group_label": "Sandbox",
      "group_icon": "terminal",
      "item_icon": "terminal",
      "widget_kind": "tool_toggle",
      "composer_label": "Python Exec",
      "composer_description": "Request approved Python code/script execution in the managed sandbox runtime; fail closed when no provider is ready.",
      "composer_icon": "terminal"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": false,
        "anyOf": [
          {
            "required": [
              "code"
            ]
          },
          {
            "required": [
              "script_path"
            ]
          }
        ],
        "properties": {
          "code": {
            "type": "string",
            "minLength": 1
          },
          "script_path": {
            "type": "string",
            "minLength": 1
          },
          "cwd": {
            "type": "string"
          },
          "template_id": {
            "type": "string"
          },
          "provider_id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "timeout": {
            "type": "integer"
          },
          "timeout_ms": {
            "type": "integer"
          }
        }
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.sandbox_tools:python_exec"
    }
  }
}
