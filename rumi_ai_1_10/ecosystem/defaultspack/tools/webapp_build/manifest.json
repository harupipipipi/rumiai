{
  "id": "webapp_build",
  "description": "Build or mark a local artifact webapp as build-ready.",
  "config": {
    "tool_id": "webapp_build",
    "name": "Webapp Build",
    "summary": "Build or mark a local artifact webapp as build-ready.",
    "tool_category": "webapp",
    "action_type": "execute",
    "write_action": true,
    "requires_approval": true,
    "risk": "high",
    "tags": [
      "webapp",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "webapp",
      "group_label": "Web App",
      "group_icon": "monitor",
      "item_icon": "monitor",
      "widget_kind": "tool_toggle",
      "composer_label": "Webapp Build",
      "composer_description": "Build or mark a local artifact webapp as build-ready.",
      "composer_icon": "monitor"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "path": {
            "type": "string"
          },
          "command": {},
          "timeout": {
            "type": "integer"
          }
        },
        "required": [
          "path"
        ]
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.webapp_tools:webapp_build"
    }
  }
}
