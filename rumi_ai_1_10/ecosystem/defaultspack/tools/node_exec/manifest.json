{
  "id": "node_exec",
  "description": "Execute approved Node.js code/script in an isolated Docker container with only the artifact workspace mounted.",
  "config": {
    "tool_id": "node_exec",
    "name": "Node Exec",
    "summary": "Execute approved Node.js code/script in an isolated Docker container with only the artifact workspace mounted.",
    "tool_category": "sandbox",
    "action_type": "execute",
    "write_action": true,
    "requires_approval": true,
    "risk": "high",
    "tags": [
      "sandbox",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "sandbox",
      "group_label": "Sandbox",
      "group_icon": "terminal",
      "item_icon": "terminal",
      "widget_kind": "tool_toggle",
      "composer_label": "Node Exec",
      "composer_description": "Execute approved Node.js code/script in an isolated Docker container with only the artifact workspace mounted.",
      "composer_icon": "terminal"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "code": {
            "type": "string"
          },
          "script_path": {
            "type": "string"
          },
          "timeout": {
            "type": "integer"
          }
        },
        "required": []
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.sandbox_tools:node_exec"
    }
  }
}
