{
  "id": "pdf_extract_tables",
  "description": "Extract tables from a PDF artifact when optional deps are available.",
  "config": {
    "tool_id": "pdf_extract_tables",
    "name": "PDF Extract Tables",
    "summary": "Extract tables from a PDF artifact when optional deps are available.",
    "tool_category": "media",
    "action_type": "read",
    "write_action": false,
    "requires_approval": false,
    "risk": "low",
    "tags": [
      "media",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "media",
      "group_label": "Media",
      "group_icon": "image",
      "item_icon": "image",
      "widget_kind": "tool_toggle",
      "composer_label": "PDF Extract Tables",
      "composer_description": "Extract tables from a PDF artifact when optional deps are available.",
      "composer_icon": "image"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ]
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.media_tools:pdf_extract_tables"
    }
  }
}
