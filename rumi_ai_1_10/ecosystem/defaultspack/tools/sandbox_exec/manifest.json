{
  "id": "sandbox_exec",
  "description": "Execute an approved command inside the artifact workspace.",
  "config": {
    "tool_id": "sandbox_exec",
    "name": "Sandbox Exec",
    "summary": "Execute an approved command inside the artifact workspace.",
    "tool_category": "sandbox",
    "action_type": "execute",
    "write_action": true,
    "requires_approval": true,
    "risk": "high",
    "tags": [
      "sandbox",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "sandbox",
      "group_label": "Sandbox",
      "group_icon": "terminal",
      "item_icon": "terminal",
      "widget_kind": "tool_toggle",
      "composer_label": "Sandbox Exec",
      "composer_description": "Execute an approved command inside the artifact workspace.",
      "composer_icon": "terminal"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "command": {},
          "cwd": {
            "type": "string"
          },
          "timeout": {
            "type": "integer"
          }
        },
        "required": [
          "command"
        ]
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.sandbox_tools:sandbox_exec"
    }
  }
}
