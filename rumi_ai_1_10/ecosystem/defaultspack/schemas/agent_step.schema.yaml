step_id: string
title: string
description: string
tool_calls: list[tool_call]
expected_output: string
risk_level: string
requires_approval: boolean
status: enum[pending, approved, running, done, failed, skipped]
