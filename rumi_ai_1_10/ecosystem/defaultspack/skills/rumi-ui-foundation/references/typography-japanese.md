# Japanese Typography

- Use system font stacks and foundation typography roles only.
- Keep Japanese long text readable with normal letter spacing, stable line height, and wrapping instead of ellipsis on primary content.
- Numeric content should use the `numeric` role when alignment matters.
