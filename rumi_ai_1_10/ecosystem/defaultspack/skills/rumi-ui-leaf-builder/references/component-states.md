# Component States

- Implement every `requiredStates` entry in the contract.
- Include fixtures for default, long, empty, loading, and error scenarios.
- Missing required states are blocker failures and cannot be accepted.
