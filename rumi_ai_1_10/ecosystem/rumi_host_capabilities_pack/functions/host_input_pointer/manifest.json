{
  "function_id": "host_input_pointer",
  "description": "Send pointer input through the approved Rumi Viewer host broker.",
  "calling_convention": "subprocess",
  "entrypoint": "main.py:run",
  "host_execution": false,
  "risk": "high",
  "risk_level": "high",
  "requires_approval": true,
  "requires": ["host.input.pointer"],
  "caller_requires": ["user.approved.high_risk"],
  "tags": ["host", "input", "pointer", "broker"],
  "host_operation": {"operation": "host.input.pointer", "stream_allowed": false},
  "input_schema": {"type": "object", "additionalProperties": true},
  "output_schema": {"type": "object", "additionalProperties": true}
}
