{
  "function_id": "host_stream_start",
  "description": "Start an approved host stream session through the Rumi Viewer host broker.",
  "calling_convention": "subprocess",
  "entrypoint": "main.py:run",
  "host_execution": false,
  "risk": "high",
  "risk_level": "high",
  "requires_approval": true,
  "requires": ["host.stream.start"],
  "caller_requires": ["user.approved.high_risk"],
  "tags": ["host", "stream", "broker"],
  "host_operation": {"operation": "host.stream.start", "stream_allowed": true},
  "input_schema": {"type": "object", "additionalProperties": true},
  "output_schema": {"type": "object", "additionalProperties": true}
}
