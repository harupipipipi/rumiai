# Operations

Use this pack when adding or reviewing optional packs. Every new pack should declare dependencies, overlaps, defaultspack promotion criteria, and all-ok eligibility. For users, choose the narrowest bundle that covers the intended workflow. For maintainers, promote a pack into defaultspack only after contract tests, conflict policy, docs, and runtime evidence prove the behavior is core rather than optional.
