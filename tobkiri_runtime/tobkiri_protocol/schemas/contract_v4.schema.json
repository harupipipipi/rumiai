{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.tobkiri.local/io.tobkiri/contract.v4.schema.json",
  "title": "Tobkiri Contract v4",
  "type": "object",
  "additionalProperties": false,
  "required": ["contract_api_version", "contract_id", "version", "revision_digest", "owner", "status", "operations", "schema_catalog", "provider_semantics", "provenance"],
  "properties": {
    "contract_api_version": {"const": "io.tobkiri.contract.v4"},
    "contract_id": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*\\.v[1-9][0-9]*$"},
    "version": {"type": "string", "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?(?:\\+[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?$"},
    "revision_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "owner": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"},
    "status": {"enum": ["draft", "accepted", "deprecated", "sunset"]},
    "operations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["operation_id", "input_schema_digest", "output_schema_digest", "error_schema_digest", "effect_ceiling"],
        "properties": {
          "operation_id": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"},
          "input_schema_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
          "output_schema_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
          "error_schema_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
          "progress_schema_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
          "effect_ceiling": {"type": "array", "items": {"type": "string", "minLength": 1}, "uniqueItems": true},
          "scope_semantics_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
          "scope_semantics": {"enum": ["declarative", "host_broker", "opaque", "unknown"]},
          "idempotency": {
            "type": "object",
            "additionalProperties": false,
            "required": ["mode"],
            "properties": {
              "mode": {"enum": ["none", "keyed", "replayable"]},
              "key_namespace": {"type": "string"},
              "reconcile_operation_id": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"},
              "retention_seconds": {"type": "integer", "minimum": 0}
            }
          },
          "timeout_default_ms": {"type": "integer", "minimum": 1},
          "timeout_hard_max_ms": {"type": "integer", "minimum": 1}
        }
      }
    },
    "schema_catalog": {
      "type": "object",
      "propertyNames": {"pattern": "^sha256:[0-9a-f]{64}$"},
      "additionalProperties": {"type": "object"}
    },
    "provider_semantics": {
      "type": "object",
      "additionalProperties": false,
      "required": ["provider_id", "cardinality", "security", "failure", "isolation", "required_capabilities", "lifecycle"],
      "properties": {
        "provider_id": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$"},
        "cardinality": {"enum": ["one", "many", "keyed", "chain", "fanout", "optional"]},
        "security": {"enum": ["public", "internal", "sensitive", "restricted"]},
        "failure": {"enum": ["fail_closed", "isolate", "continue_chain", "best_effort"]},
        "isolation": {"enum": ["in_process", "process", "sandbox", "remote", "declarative"]},
        "required_capabilities": {"type": "array", "items": {"type": "string", "minLength": 1}, "uniqueItems": true},
        "lifecycle": {"type": "object"},
        "routing_keys": {"type": "array", "items": {"type": "string", "minLength": 1}, "uniqueItems": true},
        "instance_key": {"type": "string", "minLength": 1},
        "priority": {"type": "integer"},
        "before": {"type": "array", "items": {"type": "string"}, "uniqueItems": true},
        "after": {"type": "array", "items": {"type": "string"}, "uniqueItems": true},
        "connection": {"$ref": "#/$defs/providerConnection"}
      }
    },
    "provenance": {"$ref": "https://schemas.tobkiri.local/io.tobkiri/common.v1.schema.json#/$defs/provenance"}
  },
  "$defs": {
    "safeUiText": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512
    },
    "providerConnection": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "instance_id",
        "display_name",
        "auth_modes",
        "settings_schema",
        "secret_fields",
        "operations",
        "multi_instance"
      ],
      "properties": {
        "kind": {"const": "ai_provider"},
        "instance_id": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:[._:-][a-z0-9]+)*$"},
        "display_name": {"$ref": "#/$defs/safeUiText"},
        "description": {"$ref": "#/$defs/safeUiText"},
        "icon_id": {"type": "string", "pattern": "^[a-z][a-z0-9-]{0,63}$"},
        "auth_modes": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "pattern": "^[a-z][a-z0-9._-]{0,63}$"}
        },
        "settings_schema": {
          "type": "object",
          "required": ["type"],
          "properties": {"type": {"const": "object"}}
        },
        "ui_hints": {
          "type": "object",
          "additionalProperties": {"$ref": "#/$defs/safeUiText"}
        },
        "secret_fields": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "pattern": "^[a-z][a-z0-9_]{0,63}$"}
        },
        "endpoint_requirements": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "network_required": {"type": "boolean"},
            "local_allowed": {"type": "boolean"}
          }
        },
        "operations": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "configure": {"type": "string", "minLength": 1},
            "set_credential": {"type": "string", "minLength": 1},
            "connect_oauth": {"type": "string", "minLength": 1},
            "test": {"type": "string", "minLength": 1},
            "health": {"type": "string", "minLength": 1},
            "refresh_models": {"type": "string", "minLength": 1},
            "delete_credential": {"type": "string", "minLength": 1},
            "delete_instance": {"type": "string", "minLength": 1},
            "create_instance": {"type": "string", "minLength": 1},
            "select_profile": {"type": "string", "minLength": 1}
          }
        },
        "multi_instance": {"type": "boolean"},
        "instance_field": {"type": "string", "pattern": "^[a-z][a-z0-9_]{0,63}$"},
        "configured": {"type": "boolean"},
        "credential_present": {"type": "boolean"},
        "status": {"enum": ["not_configured", "connected", "healthy", "degraded", "error", "unavailable"]},
        "diagnostic_code": {"type": ["string", "null"], "pattern": "^[a-z0-9._-]+$"},
        "model_count": {"type": ["integer", "null"], "minimum": 0},
        "last_refresh_at": {"type": ["string", "null"], "format": "date-time"}
      },
      "allOf": [
        {
          "if": {
            "properties": {"multi_instance": {"const": true}},
            "required": ["multi_instance"]
          },
          "then": {"required": ["instance_field"]}
        }
      ]
    }
  }
}
