[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
asyncio_mode = auto
markers =
    contract: tests that define the CI contract cluster for release-critical runtime behavior
    live: tests that call external provider APIs and require explicit opt-in credentials
