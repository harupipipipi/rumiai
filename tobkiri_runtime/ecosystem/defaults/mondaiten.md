pythonのライブラリ。
アニメーションなどを統一するべき。
例外を受け入れやすくする。これらはテンプレとして。  
uiをドラックアンドドロップで置けるように。ここにinputをおいて、ここにchatみたいな。あと、この欄もassetsにして、例えばsettingpackとか入れると追加されるように。toolはsidebarにtoolを使用してaiが現在何をやっているか把握できるように。あと、場所がかぶることもあるだろうから、その辺も考えよう。toolがtooldisplayやtoolsettingなどを追加して、自動配置&ユーザーが簡単に変更ができるって感じ。バックエンドのやつはものを提供して、フロントエンドで配置。
ai_profile(model_idとかプロパティとか色々)にはなんでもプロファイルを書けるようにする。例えば入力は英語推奨とか。toolも同じ感じ。というか全てのuser_dataに置く系のやつ全て。対応するかはそれ以外のpack次第。
多分被ることがある。toolやtool_proとか。なのでちゃんと選択できるようにする。
モデルを色々できるようにする。MoAとかいろんなものを。
import用の権限作成。c
toolが必要なものが入っているかどうかを検知するpythonを渡して、trueが返されればそのtoolは使えるみたいにする。例えばpython -Vでちゃんと返ってくればOKとか。toolに限らずにするといいかもです。