import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", ".."))

from blocks._common import ok, error, not_implemented, timestamp, gen_id


def run(input_data, context):
    return not_implemented("defaults.frontend.stop")
