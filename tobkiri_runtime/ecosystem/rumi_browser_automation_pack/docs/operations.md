# Operations

Use the pack when a task needs a real page rather than static HTML inspection. Start with observation, then highlight candidate elements when uncertainty is high. For destructive actions, require an explicit stop condition and capture before/after evidence.

A browser task should be considered incomplete until the final page state proves the requested outcome. A passing click result alone is not enough; the evidence ledger must contain a visible confirmation, route change, saved state, or exported artifact.
