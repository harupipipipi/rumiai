# Browser Automation Pack Docs

These docs define the declarative surface for safe browser automation in Rumi. The pack works best with the browser companion and semantic DOM v2, but it remains useful as a planning contract even when only screenshots or basic snapshots are available.
