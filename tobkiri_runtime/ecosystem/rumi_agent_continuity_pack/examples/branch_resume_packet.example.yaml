example:
  id: branch_resume_packet
  pack_id: rumi_agent_continuity_pack
  scenario: A branch resume packet references worktree state and diff summary while
    leaving branch mutation to the coding owner.
  input_refs:
  - workspace_ref:case-file
  evidence_refs:
  - evidence:review-note
  - evidence:source-fragment
  handoff_owner: rumi_code_ide_pack
  review_gate: all_blocking_checks
  expected_result: reviewed_handoff_packet
  must_not:
  - external_action
  - runtime_execution
  - secret_material
  status: ready_for_owner_handoff
