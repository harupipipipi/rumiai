contract_fixture:
  positive_cases:
  - id: sample_calculator_manifest
    given: Validate a local calculator artifact manifest
    expected: manifest_validation_handoff_packet
  - id: mcp_approval_prompt
    given: Ask approval before first MCP call
    expected: tool_approval_handoff_packet
  - id: version_rollback
    given: Select prior artifact version
    expected: version_selector_handoff_packet
  - id: export_package
    given: Build reviewed export bundle
    expected: export_manifest_handoff_packet
  minimum_expected:
  - schema_valid
  - evidence_linked
  - handoff_scoped
