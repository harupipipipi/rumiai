example:
  id: financial_model_workbook
  pack_id: rumi_office_authoring_pack
  scenario: A financial model workbook plans tabs, formulas, validation checks, and
    blocked export until formula review passes.
  input_refs:
  - workspace_ref:case-file
  evidence_refs:
  - evidence:review-note
  - evidence:source-fragment
  handoff_owner: rumi_workspace_pack
  review_gate: all_blocking_checks
  expected_result: reviewed_handoff_packet
  must_not:
  - external_action
  - runtime_execution
  - secret_material
  status: ready_for_owner_handoff
