{
  "id": "github_search",
  "description": "Search GitHub via gh CLI when explicitly executed.",
  "config": {
    "tool_id": "github_search",
    "name": "GitHub Search",
    "summary": "Search GitHub via gh CLI when explicitly executed.",
    "tool_category": "connector",
    "action_type": "network",
    "write_action": false,
    "requires_approval": true,
    "risk": "medium",
    "tags": [
      "connector",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "connectors",
      "group_label": "Connectors",
      "group_icon": "plug",
      "item_icon": "plug",
      "widget_kind": "tool_toggle",
      "composer_label": "GitHub Search",
      "composer_description": "Search GitHub via gh CLI when explicitly executed.",
      "composer_icon": "plug"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "query": {
            "type": "string"
          },
          "kind": {
            "type": "string"
          },
          "limit": {
            "type": "integer"
          },
          "execute": {
            "type": "boolean"
          }
        },
        "required": [
          "query"
        ]
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.external_connector_tools:github_search"
    }
  }
}
