{
  "id": "workflow_run",
  "description": "Run a persisted or inline local workflow.",
  "config": {
    "tool_id": "workflow_run",
    "name": "Workflow Run",
    "summary": "Run a persisted or inline local workflow.",
    "tool_category": "workflow",
    "action_type": "execute",
    "write_action": true,
    "requires_approval": true,
    "risk": "high",
    "tags": [
      "workflow",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "workflow",
      "group_label": "Workflows",
      "group_icon": "route",
      "item_icon": "route",
      "widget_kind": "tool_toggle",
      "composer_label": "Workflow Run",
      "composer_description": "Run a persisted or inline local workflow.",
      "composer_icon": "route"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "workflow_id": {
            "type": "string"
          },
          "steps": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "tool": {
                  "type": "string"
                },
                "args": {
                  "type": "object"
                }
              }
            }
          },
          "approved": {
            "type": "boolean"
          }
        },
        "required": []
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.workflow_tools:workflow_run"
    }
  }
}
