{
  "id": "workflow_retry",
  "description": "Retry a persisted workflow.",
  "config": {
    "tool_id": "workflow_retry",
    "name": "Workflow Retry",
    "summary": "Retry a persisted workflow.",
    "tool_category": "workflow",
    "action_type": "execute",
    "write_action": true,
    "requires_approval": true,
    "risk": "high",
    "tags": [
      "workflow",
      "agent_os",
      "artifact_workspace"
    ],
    "ui": {
      "group_id": "workflow",
      "group_label": "Workflows",
      "group_icon": "route",
      "item_icon": "route",
      "widget_kind": "tool_toggle",
      "composer_label": "Workflow Retry",
      "composer_description": "Retry a persisted workflow.",
      "composer_icon": "route"
    },
    "schema": {
      "parameters": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "workflow_id": {
            "type": "string"
          },
          "approved": {
            "type": "boolean"
          }
        },
        "required": [
          "workflow_id"
        ]
      }
    },
    "execution": {
      "type": "local",
      "handler": "domain.tool.workflow_tools:workflow_retry"
    }
  }
}
