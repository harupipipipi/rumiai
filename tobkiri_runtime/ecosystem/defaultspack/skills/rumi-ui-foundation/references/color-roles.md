# Color Roles

- Use semantic roles from `foundation.json` and `tokens.css`.
- Do not introduce arbitrary hex, rgb, hsl, gradients, or decorative one-off colors in leaf candidates.
- Status colors must map to critical, warning, or positive roles.
