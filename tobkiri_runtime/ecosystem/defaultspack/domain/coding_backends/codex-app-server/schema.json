{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Codex App Server Coding Backend",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "transport": {
      "type": "string",
      "enum": ["off", "stdio", "unix", "websocket_loopback", "websocket_remote"],
      "default": "off",
      "description": "Codex App Server transport. Remote websocket transports require App Server-specific auth."
    },
    "server_url": {
      "type": "string",
      "description": "Backward-compatible local or remote Codex-style app server URL."
    },
    "base_url": {
      "type": "string",
      "description": "HTTP base URL for status/probe requests."
    },
    "websocket_url": {
      "type": "string",
      "description": "WebSocket URL for Codex App Server event and turn transport."
    },
    "unix_socket_path": {
      "type": "string",
      "description": "Local Unix socket path for the unix transport."
    },
    "ws_token_file": {
      "type": "string",
      "description": "Path to a Codex App Server WebSocket token file. Raw tokens must not appear in config or CLI arguments."
    },
    "shared_secret_file": {
      "type": "string",
      "description": "Path to a Codex App Server shared-secret file. Raw secrets must not appear in config or CLI arguments."
    },
    "workspace_root": {
      "type": "string",
      "description": "Trusted workspace root. All file and terminal operations must remain inside this root."
    },
    "executable": {
      "type": "string",
      "default": "codex",
      "description": "Codex executable path. It is launched as an argv element without a shell."
    },
    "model": {
      "type": "string",
      "description": "Account-visible model id returned by model/list."
    },
    "reasoning_effort": {
      "type": "string",
      "description": "A model-supported reasoning effort returned by model/list."
    },
    "personality": {
      "type": "string",
      "description": "Personality selected only when the model reports supportsPersonality."
    },
    "include_hidden_models": {
      "type": "boolean",
      "default": false
    },
    "permission_profile": {
      "type": "string",
      "description": "Permission profile id returned by permissionProfile/list. Mutually exclusive with legacy_sandbox."
    },
    "legacy_sandbox": {
      "type": "string",
      "enum": ["readOnly", "workspaceWrite"],
      "default": "readOnly"
    },
    "required_mcp_servers": {
      "type": "array",
      "items": {"type": "string"},
      "uniqueItems": true,
      "default": []
    },
    "experimental_api": {
      "type": "boolean",
      "default": false
    },
    "approval_mode": {
      "type": "string",
      "enum": ["server_side_required"],
      "default": "server_side_required"
    }
  },
  "required": ["workspace_root"],
  "allOf": [
    {
      "not": {
        "required": ["permission_profile", "legacy_sandbox"]
      }
    }
  ]
}
