[
  {
    "id": "goal",
    "name": "goal",
    "label": "Goal",
    "description": "Pursue a goal until a third-party evaluator confirms it is achieved. Use /goal /rich or max_iterations=rich for an unbounded loop.",
    "category": "tools",
    "visibility": "default",
    "risk": "medium",
    "modes": ["chat", "coding"],
    "args": [
      { "name": "goal", "type": "string", "required": true, "greedy": true },
      { "name": "max_iterations", "type": "string", "required": false },
      { "name": "model", "type": "string", "required": false },
      { "name": "rich", "type": "string", "required": false },
      { "name": "mode", "type": "string", "required": false }
    ],
    "execution": {
      "type": "pack_block",
      "qualified_name": "defaultspack:goal.run"
    }
  }
]
