schema: io.tobkiri.profile.v4
profile_id: defaults-modern
profile_revision: 4.0.0-local
kind: runtime_profile
locale: en

display_name:
  en: Tobkiri Defaults Modern (Tauri)
  ja: Tobkiri Defaults Modern（Tauri）

description:
  en: Local-first defaultspack composition with an exact Tauri app.shell.v1 provider.
  ja: app.shell.v1 の Tauri Provider を明示的に選ぶローカル優先の標準構成。

base:
  pack: defaults-basepack

shell:
  contract: app.shell.v1
  provider: shell.tauri.default

packs:
  - defaultspack

platform:
  os: macos
  architecture: arm64

local_first:
  default_model: stub/default
  cloud_keys_required: false
  required_secrets: []
  required_network: []

policy:
  network_default: deny
  write_actions_require_approval: true
  delete_actions_require_approval: true
  terminal_actions_require_approval: true
  git_push_requires_approval: true
  audit_level: strict
  profile_may_mint_host_authority: false

materialization:
  mode: selected_only
  unselected_contributions: remain_unmaterialized
  production_source_build: false
  production_dev_commands: forbidden

metadata:
  pack_id: defaults-basepack
  category: modern_defaults
  default_for_new_setups: true
  shell_selection_is_exact: true
