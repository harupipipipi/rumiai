{
  "function_id": "request_extension",
  "description": "Create a pending request_extension approval request for a staged pack change.",
  "requires": [],
  "caller_requires": [],
  "host_execution": true,
  "tags": ["defaultspack", "pack", "request"],
  "vocab_aliases": ["defaultspack.request_extension"],
  "risk": "medium",
  "input_schema": {
    "type": "object",
    "required": ["staging_id"],
    "properties": {
      "staging_id": {"type": "string"},
      "target_pack_id": {"type": "string"},
      "notes": {"type": "string"},
      "slot": {"type": "string"},
      "fullscreen": {"type": "boolean"},
      "exclusive": {"type": "boolean"}
    }
  },
  "output_schema": {"type": "object"},
  "calling_convention": "block"
}
