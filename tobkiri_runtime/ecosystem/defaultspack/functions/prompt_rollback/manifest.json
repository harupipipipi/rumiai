{
  "function_id": "prompt_rollback",
  "description": "Roll back a prompt to a recorded Prompt Studio version.",
  "tags": ["prompt", "editor"],
  "risk": "medium",
  "requires": ["prompt.rollback"],
  "caller_requires": [],
  "host_execution": false,
  "calling_convention": "subprocess",
  "entrypoint": "main.py:run",
  "vocab_aliases": ["defaults.prompt.rollback", "defaultspack.prompt.rollback"],
  "input_schema": {"type": "object", "additionalProperties": true},
  "output_schema": {"type": "object", "properties": {"status": {"type": "string"}, "data": {}, "error": {"type": "object"}}},
  "extensions": {"defaultspack": {"block_module": "blocks.prompt.rollback", "default_args": {}}}
}
